# Ledgr
Fast, lightweight command-line tracker to log transactions, monitor live balances, and view instant category breakdown summaries without leaving the terminal. Built with modular architecture, this local tool serves as the core engine destined for a full-scale, cross-platform app.
